'use strict';

module.exports = {
  extends: 'recommended',
  rules: {
    // style
    quotes: 'double',

    // enabled rules (in addition to 'recommended')
    'bare-strings': true,
    'inline-styles': true,
    'no-duplicate-attributes': true
  }
};
