{{#ma-header
  back="routes.application.title"
  backTo="application"
  title="routes.login.title"}}
  <a href="/signup" title="Sign Up">Sign Up</a>
{{/ma-header}}

<p>
  <button onclick={{route-action "loginTwitter"}} data-test="twitter">
    Login with Twitter 🐦<br>
    <small>We won’t post without asking.</small>
  </button>
  {{#if errorMessageKeys}}
    <span>{{t errorMessageKeys}}</span>
  {{/if}}
</p>

{{ma-form-signup
  title=(t "routes.login.cta-signup")
  components=(hash
    login=(component "ma-form-login")
    input=(component "ma-input"))
  errorMessageKeys=errorMessageKeys
  loginAction=(route-action "login" model)
  action=(route-action "signup")
  model=model}}

{{outlet}}
